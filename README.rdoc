== README
http://secret-fjord-1158.herokuapp.com/

I had a huge problem this week with the params.require() function. I solved this problem by using params.fetch() instead of params.require().

<tt>rake doc:app</tt>.
